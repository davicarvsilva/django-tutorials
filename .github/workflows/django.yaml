name: Basic Workflow

on:
  push:
    branches: [ Estrutura_Inicial_do_Projecto ]
  pull_request:
    branches: [ Estrutura_Inicial_do_Projecto ]

jobs:
  example:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run a one-line script
      run: echo "GitHub Action has finished running!" 
